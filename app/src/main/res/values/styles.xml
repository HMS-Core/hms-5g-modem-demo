<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="OverflowMenuStyle" parent="@style/Widget.AppCompat.PopupMenu.Overflow">
        <!-- Whether to cover the anchor point, the default is true, that is to cover the Toolbar -->
        <item name="overlapAnchor">false</item>
        <item name="android:dropDownWidth">wrap_content</item>
        <item name="android:paddingRight">5dp</item>

        <!-- Popup layer background color -->
        <item name="android:popupBackground">@android:color/white</item>
        <!-- Set the text color of the pop-up menu -->
        <item name="android:textColor">@android:color/holo_green_dark</item>

        <!-- The vertical offset of the pop-up layer, that is, the distance from the Toolbar in the
        vertical direction. A negative value will cover the Toolbar -->
        <item name="android:dropDownVerticalOffset">5dp</item>
        <!-- The horizontal offset of the pop-up layer, that is, the distance from the left of the
        screen, a negative value will cause a gap on the right-->
        <item name="android:dropDownHorizontalOffset">0dp</item>
    </style>

    <style name="AppTheme.NoBar">
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AlertDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="RadioButtonStyle" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/darkGrayBackground</item>
        <item name="colorControlActivated">@color/greenColor</item>
    </style>

    <style name="CheckBoxStyle" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/darkGrayBackground</item>
        <item name="colorControlActivated">@color/greenColor</item>
    </style>

    <style name="EquipmentItemStyle" parent="@android:style/Theme.Holo.Light">
        <item name="android:textSize">12sp</item>
    </style>
</resources>
